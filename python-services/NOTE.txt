extract_structured_content:

    ساختار خروجی: یک دیکشنری ایجاد می‌کنیم که شامل صفحات، جداول، تصاویر و بخش‌ها باشد

    حلقه روی صفحات: هر صفحه را یکی‌یکی پردازش می‌کنیم

    تشخیص ستون‌ها: برای هر صفحه تشخیص می‌دهیم که یک ستونه یا دو ستونه

    استخراج متن: متن را بر اساس ستون‌ها استخراج می‌کنیم

    استخراج تصاویر: تصاویر درون صفحه را استخراج می‌کنیم

    استخراج جداول: جداول را تشخیص می‌دهیم (ساده)

    بستن فایل: بعد از اتمام کار، فایل را می‌بندیم